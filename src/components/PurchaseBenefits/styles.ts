import styled from 'styled-components';

export const HomePurchaseBenefits = styled.div`
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-top: 4rem;

  div {
    background: var(--yellow-dark);
    padding: 0.5rem;
    border-radius: 50%;
    width: 32px;
    height: 32px;
  }

  svg {
    color: var(--white);
  }
`;
